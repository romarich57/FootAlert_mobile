import { StyleSheet } from 'react-native';

export const playerDetailsScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  center: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  offlineBannerWrap: {
    paddingHorizontal: 16,
    paddingBottom: 8,
  },
});
