# API Contract

- Source contract: `openapi/footalert.v1.yaml`
- Generated types: `generated/types.ts`

## Commands

```bash
npm run lint
npm run generate
npm run check
```

`check` validates schema, regenerates types, and fails if generated output is not committed.
