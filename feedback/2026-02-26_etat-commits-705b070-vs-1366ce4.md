# Notes d'état des commits (26-02-2026)

## Contexte
- Commit A (ancien/stable): `705b070`
- Commit B (dernier problématique): `1366ce4`
- État actuel de `main` après rollback: `98772c3` (`Revert "test"`)

## Commit `705b070` — état fonctionnel

### Nature des changements
- Portée ciblée (13 fichiers).
- Stabilisation des flux `TeamDetails`, `More`, telemetry et BFF teams standings.

### Effets principaux observés
- `TeamDetails`: meilleure gestion loading/error côté stats + standings.
- BFF `teams/standings`: accepte correctement les réponses vides (`response: []`) sans crash.
- Télémétrie: réduction du bruit (429 attendus), warnings plus pertinents sur erreurs 5xx réseau.
- `More`: optimisation de chargement (catalogue devise chargé à la demande).

### Évaluation
- Commit orienté robustesse incrémentale.
- Risque de régression faible à modéré (changements localisés).

## Commit `1366ce4` — état fonctionnel

### Nature des changements
- Très large refactor transverse (44 fichiers).
- Introduction d'une couche `availability` partagée.
- Refonte de la logique d'onglets et états data sur `Competitions`, `Follows`, `Players`, `Teams`.

### Effets principaux observés
- Changement comportemental massif des écrans dépendants des données.
- Réintroduction de `initialTab` dans la navigation `TeamDetails`.
- Surface de régression élevée (UI + navigation + disponibilité des onglets).

### Évaluation
- Commit structurel ambitieux mais risqué en un seul lot.
- Probabilité de bugs fonctionnels élevée sans rollout progressif.

## État actuel réel du projet
- `main` contient `98772c3` (revert du commit `1366ce4`).
- Le contenu code est revenu à l'état fonctionnel de `705b070`.
- Vérification effectuée précédemment: `git diff 705b070..98772c3` vide.

## Recommandation
- Garder `705b070` comme base stable.
- Reprendre les idées de `1366ce4` en lots plus petits (feature par feature), avec tests de non-régression ciblés à chaque étape.
